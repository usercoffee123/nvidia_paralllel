[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    slow: tests that spawn joblib subprocesses or run many circuits
    gpu: tests that require an NVIDIA GPU and run on the CUDA-Q nvidia target
