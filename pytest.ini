[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    slow: tests that run many CUDA-Q circuits
    gpu: tests that require an NVIDIA GPU and run on the CUDA-Q nvidia target
